<domain-models xmlns="http://axelor.com/xml/ns/domain-models"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://axelor.com/xml/ns/domain-models
  http://axelor.com/xml/ns/domain-models/domain-models_5.4.xsd">

  <module name="project" package="com.axelor.apps.project.db"/>

  <entity name="ProjectTask" cacheable="true">
  	<string name="name" title="Task Summary" required="true"/>
	<string name="notes" multiline="true"/>
	<string name="state" selection="projectTask-state-selection"/>
	<integer name="priority"/>
	<datetime name="createDate"/>
	<datetime name="startDate"/>
	<datetime name="endDate"/>
	<datetime name="deadlineDtae"/>
	<integer name="sequence"/>	
	<integer name="progress">
	<![CDATA[
      double result = 0.0, duration = 0.0;
    if (workItems == null || startDate == null || endDate == null) {
      return 0;
    }
    for(ProjectWork work : workItems) {
      java.time.LocalTime time = work.getHours();
      result += time.getHour();
      result += time.getMinute() / 60.0;
    }
    duration = java.time.temporal.ChronoUnit.HOURS.between(startDate, endDate);

    result = Math.min(100.0, ((result / duration) * 100));
    return (int) Math.round(result);
    ]]>
    </integer>
		<many-to-one  name="project" ref="com.axelor.apps.project.db.Project" />
			<many-to-one name="user" column="user_id" ref="com.axelor.auth.db.User" required="true" />
				<one-to-many  name="workItems" ref="com.axelor.apps.project.db.ProjectWork" mappedBy="task"/>
					<many-to-one name="parentTask" ref="com.axelor.apps.project.db.ProjectTask" />
  </entity>
</domain-models>